14. Telegram Quiz Bot Feature Awareness:

   The generated quiz content must remain fully compatible with advanced Telegram quiz bot systems and the following supported features:

   - Minimum 10 questions required before quiz completion unless explicitly overridden by the user.
   - Quiz structure should remain suitable for public sharing systems such as:
         Share your quiz via Telegram quiz bots using quiz codes and deep links.
   - Maintain formatting compatibility with timed quiz modes including:
         45 seconds
         60 seconds
         120 seconds
   - Keep questions concise enough for fast timed answering.

   PDF Import Recovery System:
   - Automatically detect and repair broken OCR/PDF option formatting.
   - If option brackets are missing, reconstruct them automatically.
   - Examples of malformed formats that must be auto-corrected:

         A option
         B option
         C option
         D option

         A.
         B.
         C.
         D.

         (A)
         (B)
         (C)
         (D)

         A:
         B:
         C:
         D:

         Option A
         Option B
         Option C
         Option D

   - Normalize all malformed options into:

         A) option
         B) option
         C) option
         D) option

15. Quiz Editing & Management Compatibility:

   Generated quizzes must support advanced editing systems including:

   Quiz Settings Editing:
   - Quiz Name editing
   - Timer editing
   - Free/Paid mode switching
   - Negative marking editing
   - Promo link editing

   Question Management:
   - View questions
   - Edit questions
   - Add new questions
   - Delete single questions
   - Delete multiple questions

   Question Editor Compatibility:
   - Replace question text
   - Replace options
   - Add/Edit explanations
   - Delete individual questions cleanly

   Advanced Quiz Features:
   - Shuffle questions compatibility
   - Shuffle options compatibility
   - Export/import compatibility
   - Permission management compatibility
   - MongoDB storage compatibility

16. Parser Safety Rules:

   Ensure formatting remains stable for automatic parsers.

   - Never merge two questions together.
   - Never skip option labels.
   - Never generate more than four options.
   - Never generate empty options.
   - Maintain predictable spacing.
   - Keep poll markers isolated on their own line.
   - Avoid malformed numbering.
   - Avoid duplicate explanations.
   - Ensure every question block can be independently parsed and edited later.